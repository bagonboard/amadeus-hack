const { test, chekin, mostTraveled, lower, lowerPrices, lowerPricesCache, searchFare } = require('./controller');

module.exports = {
  test,
  chekin,
  mostTraveled,
  lower,
  lowerPrices,
  lowerPricesCache,
  searchFare,
};
