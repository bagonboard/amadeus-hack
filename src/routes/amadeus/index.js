const { booking, test, find, chekin, mostTraveled, lower, lowerPrices, lowerPricesCache } = require('./controller');

module.exports = {
  booking,
  test,
  find,
  chekin,
  mostTraveled,
  lower,
  lowerPrices,
  lowerPricesCache,
};
